/*
 * name: Jeff spacek
 * Date: winter 2026
 * Description: Main class for triangle and Triangles_Recursion
 * classes.
 * 
 */

package assignment_01;

public class myTriangle {

	public static void main(String[] args) {
		int screenWidth = 900;
		int screenHeight = 800;
		int howManyTriangles = 10;
		int triangleMaxSize = 50;

		// comment out / in for different classes/effects
		// triangle SR = new triangle(screenWidth, screenHeight, howManyTriangles,
		// triangleMaxSize); // none recurcive

		Triangles_Recursion SR = new Triangles_Recursion(screenWidth, screenHeight, howManyTriangles, triangleMaxSize);

		SR.start();
	}

}